$(function (){
/*	$.get([[@{/user/userDistribution}]] "/user/userDistribution",function (data){
		// 基于准备好的dom，初始化echarts实例
		var myChart = echarts.init($('#main')[0]);

		var header = [];
		var value = [];

		for(var v of data){
			header.push(v.age);
			value.push(v.count);
		}

		// 指定图表的配置项和数据
		var option = {
				title: {
					text: '用户分布情况'
				},
				tooltip: {},
				legend: {
					data:['销量']
				},
				xAxis: {
					data: header
				},
				yAxis: {},
				series: [{
					name: '销量',
					type: 'bar',
					data: value
				}]
		};

		// 使用刚指定的配置项和数据显示图表。
		myChart.setOption(option);
	});*/
});