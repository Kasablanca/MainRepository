server:
  port: 8082
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/meeting?characterEncoding=UTF-8
    username: mwd
    password: mwd
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    open-in-view: true
  profiles:
    active:
    - prod
    - test
logging:
  level:
    root: warn